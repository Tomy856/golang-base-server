# 環境境界：実行・デバッグルール

## 1. 実行場所の禁止事項
- **AI内部サンドボックスでの実行禁止**: `bwrap`, `socat`, `rg` 等の不足によるエラー（Exit code 1等）を出すな。
- **自律実行の制限**: AI自身がテストを回そうとせず、常に「ユーザーへのコマンド提示」に徹せよ。

## 2. 開発コンテナでの操作
- 常に `docker compose exec app ...` 形式のコマンドを生成せよ。
- ユーザーからの実行結果（ログ）を受け取ってから、次の「一括更新フロー」へ進め。