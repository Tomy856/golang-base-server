# airホットリロード運用

## 概要
`air` はアプリ実行ホットリロード用で `godoc` とは別扱いとします。

## 依頼フレーズ
- `airでビルドして`
- `air再起動して`

## 実行コマンド（例）
```bash
cd /server
air
```

## 補足
`air` は `godoc` のドキュメント再生成はしないため、コード変更後に `godoc` も更新する場合は `chat-cmd/godoc.md` の手順を実行します。
